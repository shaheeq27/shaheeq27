requests==2.32.3
beautifulsoup4==4.12.3
# portrait-only (not needed by the daily workflow — only fetch_contributions.py
# and render_heatmap_svg.py run in CI, and neither imports these):
pillow
numpy
opencv-python
rembg
